import fs from 'fs';

// Reemplazamos la siguiente clase ProductManager por la que realizaste la entrega pasada
// Friendly Remember: Los productos ahora se agregaran con propiedades extras (revisar nuevamente la presentación del desafío)
class ProductManager {
  constructor(path) {
    this.path = path
  };
}

export default ProductManager